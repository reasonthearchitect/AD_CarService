spring:
  application:
    name: ${SPRING_APPLICATION_NAME:carstore}
  cloud:
    config:
      enabled: false

---
spring:
  profiles: prod
  cloud:
      config:
        enabled: false
        uri: http://172.31.55.162:8888